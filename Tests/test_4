main
var x, y;
{
    let x <- 51; 
    let y <- 10;
    while x > 0 do
        if x > y then
            call outputnum(y);
            call outputnewline();
        fi;
        let x <- x - 5;
        call outputnum(x);
        call outputnewline();
    od;
    call outputnum(y);
}.
