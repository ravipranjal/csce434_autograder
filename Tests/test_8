main
var temp, for, outputnum;
{
	let temp <- 2;
	let for <- 3;
	call outputnum(temp * for + 3*for - temp);
	call outputnewline();
	let outputnum <- call inputnum();
	call outputnum(outputnum);
	call outputnewline();
}. 
