main
var curr, prev, next, limit;
{
    let curr <- 1;
    let prev <- 0;
    let limit <- call inputnum();
    while curr < limit do
        call outputnum(curr);
        call outputnewline();
        let next <- prev + curr;
        let prev <- curr;
        let curr <- next;
    od; 
}.
