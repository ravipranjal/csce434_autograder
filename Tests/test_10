//Basic if statement test
main
var a, b;
{
	let a <- 2410;
	let b <- 4256;
	if a > b then
		let a <- a + 1;
		let b <- a;
	else
		let b <- b + 3;
		let a <- call inputnum();
		let a <- (b + b)* 1; 
	fi;
	
	if a < b then
		let a <- a - 2
	fi;
	
	call outputnum(a);
	call outputnum(b)
}.