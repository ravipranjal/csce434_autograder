main
var a, b;
{
	let a <- 1;
	while a < 6 do
        let b <- 2;
        while b < 6 do 
            if a+b == 3 then
                call outputnum(a);
                call outputnewline();
            fi;

            if a+b == 4 then
                call outputnum(b);
                call outputnewline();
            fi;

            if a+b == 5 then
                call outputnum(a);
                call outputnewline();
            fi;

            let b <- b + 1;
        od;
        let a <- a + 1;
    od;
}.
