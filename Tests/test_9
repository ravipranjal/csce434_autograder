main
var input, myvar;
function tailrecursum(n,i)
{	
	if n == 0 then
		return i;
	fi;
	return call tailrecursum(n-1, i + n);
};

{
    let input <- call inputnum();
    let myvar <- call tailrecursum(input, 0);
    call outputnewline();
    call outputnum(myvar);
    call outputnewline();
}.
