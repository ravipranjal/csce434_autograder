main
var input, myvar;

function gettwosum(n)
var i,k;
{
    let i <- call inputnum();
    let k <- i + n;
    return k;
};

{
    let input <- call inputnum();
    let myvar <- call gettwosum(input);
    call outputnum(myvar);
    call outputnewline();
}.
